<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">
    <item
        android:id="@+id/new_meeting"
        android:title="@string/new_meeting_menu_title"/>
    <item
        android:id="@+id/find"
        android:title="@string/find_meeting_menu_title"/>
    <item
        android:id="@+id/about"
        android:title="@string/about_menu_title"/>
    <item
        android:id="@+id/exit"
        android:title="@string/exit_menu_title"/>
</menu>
